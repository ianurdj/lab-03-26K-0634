#include <stdio.h>

int main()
{
    float km, m, cm;

    printf("Enter distance in km: ");
    scanf("%f", &km);

    m = km * 1000;
    cm = m * 100;

    printf("In meters: %f\n", m);
    printf("In centimeters: %f\n", cm);

    return 0;
}